import { SmartTableColumnLabelPipe } from './smart-table-column-label.pipe';

describe('SmartTableColumnLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartTableColumnLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
